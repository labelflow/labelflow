/**
 * Number of digits which can be displayed per root em
 * @remarks {@link https://github.com/labelflow/labelflow/pull/693#discussion_r776972156}
 */
export const DIGITS_PER_REM = 0.55;

/**
 * Default number of items per page which can be selected by the
 * {@link PerPageInput} component
 */
export const DEFAULT_PER_PAGE_OPTIONS = [10, 25, 50, 100];
