{
  "name": "infrastructure",
  "scripts": {
    "deploy": "dotenv -e .env.local -- pulumi up -d",
    "destroy": "dotenv -e .env.local -- pulumi destroy"
  },
  "devDependencies": {
    "@types/node": "10.0.0",
    "dotenv-cli": "4.0.0"
  },
  "dependencies": {
    "@kubernetes/client-node": "0.16.3",
    "@pulumi/cloudflare": "4.0.0",
    "@pulumi/gcp": "5.21.0",
    "@pulumi/kubernetes": "3.7.2",
    "@pulumi/pulumi": "3.13.2",
    "@pulumi/tls": "4.0.0"
  }
}
