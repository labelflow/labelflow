input ImageCreateInput {
  id: ID
  name: String
  path: String
  mimetype: String
  height: Int
  width: Int
  file: Upload # Either file or url are needed
  url: String # Either file or url are needed
}

input ImageWhereInput {
  id: ID
}

input ImageWhereUniqueInput {
  id: ID!
}
