schema: "./data/*.graphql"
generates:
  ./typescript/web-app/src/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./data/generated-schema.graphql:
    plugins:
      - schema-ast
